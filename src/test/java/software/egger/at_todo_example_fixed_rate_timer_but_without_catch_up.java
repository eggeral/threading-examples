package software.egger;

public class at_todo_example_fixed_rate_timer_but_without_catch_up {

    // example where tasks do not queue up when they are executed peridically but they take longer than the period.

}
